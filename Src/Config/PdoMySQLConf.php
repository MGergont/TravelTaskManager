<?php

declare(strict_types=1);

namespace Src\Config;

return [
    'db' => [
        'host' => DB_HOST,
        'database' => DB_NAME,
        'port' => DB_PORT,
        'user' => DB_USER,
        'password' => DB_PASS,
    ]
];
